version: '3.8'

services:
  repuragent:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - ./.env:/app/.env
      - ./backend/memory:/app/backend/memory
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
    networks:
      - repuragent-network

networks:
  repuragent-network:
    driver: bridge
